<%= form_with(model: skater) do |form| %>
  <% if skater.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(skater.errors.count, "error") %> prohibited this skater from being saved:</h2>

      <ul>
        <% skater.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :f_name, style: "display: block" %>
    <%= form.text_field :f_name %>
  </div>

  <div>
    <%= form.label :l_name, style: "display: block" %>
    <%= form.text_field :l_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
